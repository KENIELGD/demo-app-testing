import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo-app-calculator-testing',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
